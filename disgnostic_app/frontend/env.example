# Base URL used by the React app to reach the FastAPI backend.
VITE_API_BASE_URL=http://localhost:8000

# Optional overrides for the Vite dev server.
# VITE_DEV_SERVER_HOST=0.0.0.0
# VITE_DEV_SERVER_PORT=5173

